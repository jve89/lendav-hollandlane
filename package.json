{
  "name": "lennupesu",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "links": "node scripts/check-html.mjs",
    "verify": "npm run check && npm run build && npm run links"
  },
  "dependencies": {
    "astro": "^7.1.3",
    "@astrojs/sitemap": "^3.7.3"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "@astrojs/check": "^0.9.4"
  }
}
